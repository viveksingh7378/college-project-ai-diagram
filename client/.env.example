# Client-side environment variables.
# Copy to `.env` or `.env.local` for local dev.
#
# For local dev (outside Docker), point to the local Node server:
VITE_API_URL=http://localhost:5000/api

# When the client is served by nginx inside Docker, the Dockerfile build
# sets this to "/api" so nginx proxies requests to the server container.
